{
    "scripts": {
        "dev": "concurrently \"cd ./backend && npm run server\" \"cd ./frontend && npm run dev\"",
        "postinstall": "(npm install) && (cd frontend && npm install); (cd backend && npm install)"
    },
    "devDependencies": {
        "@typescript-eslint/eslint-plugin": "^5.59.0",
        "@typescript-eslint/parser": "^5.59.0",
        "eslint": "^8.39.0",
        "eslint-plugin-react": "^7.32.2"
    },
    "dependencies": {
        "concurrently": "^8.0.1",
        "postinstall": "^0.8.0",
        "prettier": "^3.0.2"
    }
}
